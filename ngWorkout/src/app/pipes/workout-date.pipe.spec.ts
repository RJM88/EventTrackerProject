import { WorkoutDatePipe } from './workout-date.pipe';

describe('WorkoutDatePipe', () => {
  it('create an instance', () => {
    const pipe = new WorkoutDatePipe();
    expect(pipe).toBeTruthy();
  });
});
